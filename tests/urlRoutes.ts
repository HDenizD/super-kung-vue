const baseUrl = 'http://localhost:3000/'
const playerNameUrl = `${baseUrl}game/player-name`
const stageHubUrl = `${baseUrl}game/stage-hub`
const stage1Url = `${baseUrl}game/stage-1`
const gameOverUrl = `${baseUrl}game/over`

export { baseUrl, playerNameUrl, stageHubUrl, stage1Url, gameOverUrl }
